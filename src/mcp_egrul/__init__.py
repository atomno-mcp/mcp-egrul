"""mcp-egrul — MCP-сервер для ЕГРЮЛ/ЕГРИП РФ.

Публичный контракт пакета — набор async-тулзов, регистрируемых в `server.py`.
Версия фиксируется здесь и читается в pyproject.toml при публикации (см.
`hatchling.build`).
"""

__version__ = "0.1.8"
